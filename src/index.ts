import {
  CommandsRegistry,
  handlerLogin,
  registerCommand,
  runCommand,
} from "./commands.js";

function main() {
  // TODO: create a CommandsRegistry
  // TODO: register the login handler
  // TODO: parse process.argv — slice off node and script path
  // TODO: if no arguments, print error and exit with code 1
  // TODO: split into command name + args, call runCommand, catch and print errors
}

main();
